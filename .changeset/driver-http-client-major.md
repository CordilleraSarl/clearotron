---
"prelim-driver": patch
---

For operators: The HTTP client the engine uses for outbound calls moves to its current major version, picking up its security and maintenance fixes. Behaviour is unchanged.
