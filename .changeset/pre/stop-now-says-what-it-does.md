---
"clearotron-driver": patch
---

Fixed: The notice shown while a run is stopping no longer overlaps the elapsed time on the card. It has its own line under the run, so "1 min so far" stays readable while a stop is in flight.
