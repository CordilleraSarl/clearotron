---
"clearotron-driver": patch
---

Fixed: The run purge no longer deletes a clearance whose report or failure notice has not been sent. Owed runs are marked in the table it prints, deleting one takes a flag that says what is being given up, and every applied purge now leaves a record of what it removed.
