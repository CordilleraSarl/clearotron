---
"clearotron-driver": patch
---

Fixed: Closing the terminal window that runs `clearotron start` now stops everything it started, as its start message says, instead of leaving it running unsupervised.
